apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-shift-planner.fullname" . }}
  labels:
    {{- include "ai-shift-planner.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-shift-planner.selectorLabels" . | nindent 4 }}
